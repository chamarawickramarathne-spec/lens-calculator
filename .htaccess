# Lens Calculator - Apache Configuration

# Enable Rewrite Engine
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /lens-calculator/
    
    # Redirect to index.html if accessing directory
    DirectoryIndex index.html
</IfModule>

# Security Settings
<IfModule mod_headers.c>
    # Prevent clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # XSS Protection
    Header always set X-XSS-Protection "1; mode=block"
    
    # Content Type Options
    Header always set X-Content-Type-Options "nosniff"
</IfModule>

# PHP Settings
<IfModule mod_php7.c>
    php_value upload_max_filesize 10M
    php_value post_max_size 10M
    php_value max_execution_time 60
    php_value max_input_time 60
</IfModule>

# MIME Types
<IfModule mod_mime.c>
    AddType application/json .json
    AddType text/css .css
    AddType text/javascript .js
</IfModule>

# Compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Browser Caching
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
</IfModule>

# Error Pages (Optional - create these files if needed)
# ErrorDocument 404 /lens-calculator/404.html
# ErrorDocument 403 /lens-calculator/403.html
# ErrorDocument 500 /lens-calculator/500.html

# Protect sensitive files (Apache 2.4 syntax)
<FilesMatch "^(config\.php|\.htaccess|\.git)">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Allow access to API files
<FilesMatch "^(add_equipment|generate_pdf|generate_package_pdf|get_categories|get_equipment|get_equipment_types|get_templates|log_access|save_package)\.php$">
    <IfModule mod_authz_core.c>
        Require all granted
    </IfModule>
</FilesMatch>
